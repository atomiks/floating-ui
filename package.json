{
  "name": "@floating-ui/monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Floating UI monorepo",
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "dev": "concurrently 'npm -w packages/position run dev' 'npm -w packages/dom run dev'",
    "build:typescript": "tsc --project .config/tsconfig.json",
    "clean": "rm -rf packages/**/src/[^_]*.d.ts packages/**/src/**/[^_]*.d.ts"
  },
  "author": "",
  "license": "MIT",
  "prettier": {
    "singleQuote": true,
    "bracketSpacing": false,
    "proseWrap": "always"
  },
  "devDependencies": {
    "prettier": "^2.5.0",
    "typescript": "^4.5.2"
  }
}
