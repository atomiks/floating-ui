import { Modifier } from '../types';
import { Options as DetectOverflowOptions } from '../utils/detectOverflow';
export declare type Options = DetectOverflowOptions;
export declare const size: (options?: Partial<Options>) => Modifier;
